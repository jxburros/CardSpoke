interface:
  display_name: "Generated Artifact and Export Contract Checker"
  short_description: "Use when changing export formats, schema versions, or import round trips."
  default_prompt: "Use $generated-artifact-export-contract-checker to work safely on the relevant repository task."
policy:
  allow_implicit_invocation: true
